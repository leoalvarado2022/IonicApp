import { NumericOrderPipe } from './numeric-order.pipe';

describe('NumericOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new NumericOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
